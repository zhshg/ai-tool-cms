/**
 * Database seed entry (scaffold).
 * Seed implementations are added in subsequent commits.
 */
async function main(): Promise<void> {
  // no-op
}

main()
  .then(() => process.exit(0))
  .catch((error: unknown) => {
    console.error(error);
    process.exit(1);
  });
